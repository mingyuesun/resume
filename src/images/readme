1. 如图，请用代码实现设计图中的功能, 需求如下：

  1) 在输入框里输入英文单词，不同单词用空格分开，输入过程中内容实时匹配元素周期表中的元素，比如：输入`holly`匹配的元素为："Ho"、"H"、"O", 按照设计图中的样式展示，如果有多组元素匹配，可以在下拉框里进行选择展示；

  2) 输入框里输入多个不同单词后默认按照单词数量平均分为两组内容，如: `holly okay white`分为两组：`holly okay`和`white`, 分别按照设计图展示；

  3) 请实现一个算法, 用来在输入内容中匹配元素周期表的元素
